{{<layout}}
{{$title}}{{post.title}}{{/title}}
{{$content}}
<article>
    <header>
        <h2>{{post.title}}</h2>
        <p>
            <time>{{post.dateStr}}</time>
            {{#post.tags}}{{.}} {{/post.tags}}
        </p>
    </header>
    <section>
        {{{post.content}}}
    </section>
</article>
{{/content}}
{{/layout}}
